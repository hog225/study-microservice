server.port: 7003
server.error.include-message: always

logging:
  level:
    root: INFO
    se.yg: DEBUG

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---
spring.profiles: docker

server.port: 8080